# PRD: 오늘의 할일 관리 웹페이지

## 1. 개요

### 제품 목적
오늘 해야 할 일을 빠르게 기록하고, 완료 여부를 추적하며, 하루를 효율적으로 마무리할 수 있도록 돕는 단일 페이지 웹 애플리케이션.

### 목표 사용자
- 별도 가입 없이 바로 사용하고 싶은 개인 사용자
- 단순하고 빠른 할일 관리 도구가 필요한 사용자

### 핵심 가치
- **단순함**: 배울 필요 없이 바로 사용 가능
- **빠름**: 키보드만으로 할일 추가·완료·삭제 가능
- **지속성**: 새로고침 후에도 데이터 유지 (localStorage)

---

## 2. 기능 요구사항

### 2.1 필수 기능 (MVP)

| 기능 | 설명 |
|------|------|
| 할일 추가 | 텍스트 입력 후 Enter 또는 버튼 클릭으로 추가 |
| 완료 체크 | 체크박스 클릭으로 완료/미완료 토글 |
| 할일 삭제 | 개별 항목 삭제 버튼 |
| 날짜 표시 | 오늘 날짜를 헤더에 표시 |
| 진행률 표시 | "3/7 완료" 형식의 요약 및 진행 바 |
| 데이터 유지 | localStorage를 사용해 새로고침 후에도 데이터 보존 |

### 2.2 선택 기능 (v2 후보)

| 기능 | 설명 |
|------|------|
| 우선순위 설정 | 높음 / 보통 / 낮음 레이블 |
| 필터링 | 전체 / 진행중 / 완료 탭 |
| 전체 완료 처리 | "모두 완료" 버튼 |
| 완료 항목 일괄 삭제 | "완료 항목 지우기" 버튼 |
| 드래그앤드롭 정렬 | 항목 순서 변경 |

---

## 3. 비기능 요구사항

- **반응형 디자인**: 모바일(360px) ~ 데스크톱(1440px) 지원
- **단일 파일**: 별도 서버 없이 `index.html` 하나로 동작
- **외부 의존성 없음**: CDN 포함 외부 라이브러리 사용 안 함 (순수 HTML/CSS/JS)
- **접근성**: 키보드 탐색 및 스크린리더 기본 지원 (`aria-label`, `role` 속성)
- **성능**: 초기 로딩 1초 이내, 인터랙션 응답 100ms 이내

---

## 4. UI/UX 설계

### 4.1 레이아웃 구성

```
┌──────────────────────────────────┐
│  📅 2026년 6월 5일 목요일         │  ← 헤더 (날짜)
│  3 / 7 완료   ████░░░░ 43%       │  ← 진행률 바
├──────────────────────────────────┤
│  [ 할일을 입력하세요...    ] [추가] │  ← 입력 영역
├──────────────────────────────────┤
│  ☐  프로젝트 기획서 작성          │  ← 할일 목록
│  ☑  팀 미팅 참석           [삭제] │
│  ☐  코드 리뷰              [삭제] │
│  ...                             │
└──────────────────────────────────┘
```

### 4.2 색상 및 스타일

- **배경**: 밝은 그레이 (`#f5f5f5`)
- **카드**: 흰색 (`#ffffff`), 둥근 모서리, 그림자
- **완료 항목**: 텍스트에 취소선, 색상 흐리게
- **진행 바**: 완료율에 따라 색상 변화 (0~49%: 파랑, 50~99%: 초록, 100%: 금색)
- **폰트**: 시스템 기본 폰트 스택 (`-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`)

---

## 5. 데이터 구조

```json
{
  "todos": [
    {
      "id": "uuid-v4",
      "text": "프로젝트 기획서 작성",
      "completed": false,
      "createdAt": "2026-06-05T09:00:00Z"
    }
  ],
  "lastUpdated": "2026-06-05T09:00:00Z"
}
```

- 저장 키: `localStorage["today-todos"]`
- 날짜가 바뀌면 자동으로 목록 초기화 여부를 사용자에게 확인

---

## 6. 기술 스택

| 구분 | 선택 |
|------|------|
| 마크업 | HTML5 |
| 스타일 | CSS3 (Flexbox, CSS Variables) |
| 로직 | Vanilla JavaScript (ES6+) |
| 저장소 | Web localStorage API |
| 빌드 도구 | 없음 (단일 파일) |

---

## 7. 파일 구조

```
to-do-list/
├── PRD.md          ← 이 문서
└── index.html      ← 단일 구현 파일 (HTML + CSS + JS 인라인)
```

---

## 8. 성공 지표

- 할일 추가까지 소요 클릭/키 입력: **2회 이하** (텍스트 입력 + Enter)
- 첫 화면 렌더링: **1초 이내**
- 새로고침 후 데이터 유지 여부: **100%**

---

## 9. 개발 범위 및 우선순위

| 단계 | 항목 | 우선순위 |
|------|------|----------|
| MVP | 할일 추가 / 완료 / 삭제 | P0 |
| MVP | 날짜 표시 + 진행률 바 | P0 |
| MVP | localStorage 저장 | P0 |
| MVP | 반응형 레이아웃 | P1 |
| v2 | 필터 탭 (전체/진행/완료) | P2 |
| v2 | 완료 항목 일괄 삭제 | P2 |
| v2 | 드래그앤드롭 정렬 | P3 |

---

## 10. 제약 사항

- 서버 사이드 처리 없음 — 순수 클라이언트 사이드
- 다중 기기 동기화 없음 (로컬 브라우저 저장만)
- 로그인/계정 기능 없음
